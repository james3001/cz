<h2>Categorias:</h2>

<? foreach($this->mensajes as $producto): ?>
        <li><?=$producto?></li>
<? endforeach; ?>
        
        Último Error: <?=$this->catErrada?>;

<a href="<?=$this->url(array('module'=>'ventas','controller'=>'categoria','action'=>'nuevo'),'default',true)?>">Nuevo</a>
<br />
<br />

<table border="1">
    <thead>
        <tr>
            <th>Nombre</th>
            <th>Desc</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        <? foreach($this->categorias as $producto): ?>
        <tr class="<?=$producto['activo']?'':'in'?>activo">
            <td><?=$producto['nombre']?></td>
            <td><?=$producto['descripcion']==''?'&nbsp;':$producto['descripcion']?></td>
            <td>
                <a href="<?=$this->url(array('module'=>'ventas','controller'=>'categoria','action'=>'editar','id'=>$producto['id']),'default',true)?>">Editar</a>
                <? if( (bool) $producto['activo'] ): ?>
                    <a href="<?=$this->url(array('module'=>'ventas','controller'=>'categoria','action'=>'desactivar','id'=>$producto['id']),'default',true)?>">Desactivar</a>
                <? else: ?>
                    <a href="<?=$this->url(array('module'=>'ventas','controller'=>'categoria','action'=>'activar','id'=>$producto['id']),'default',true)?>">Activar</a>
                <? endif; ?>
                
            </td>
        </tr>
        <? endforeach; ?>
    </tbody>
</table>

<div class="pagination all">
    <?=$this->paginationControl($this->categorias,'Sliding','categoria/_pag.phtml')?>
</div>


<style type="text/css">
    .inactivo{background-color: lightpink;}
    .activo{background-color: lightgreen;}
</style>
    